# MySQL (Docker)
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=portfolio
MYSQL_USER=portfolio
MYSQL_PASSWORD=portfoliopass
MYSQL_PORT=3011

# Приложение
APP_PORT=3000

# Для локального запуска без Docker (npm start)
DB_HOST=127.0.0.1
DB_PORT=3011
DB_NAME=portfolio
DB_USER=root
DB_PASSWORD=rootpassword

# --- DBeaver (как в проекте СоцПомощник) ---
# Тип: MySQL
# Host: localhost
# Port: 3011
# Database: portfolio
# Username: root
# Password: rootpassword
# После подключения: Databases → portfolio → Tables
